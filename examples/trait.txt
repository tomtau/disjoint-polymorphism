--> ()

trait Point(x : Int, y: Int) { self : Point =>
  def x () : Int = x;
  def y () : Int = y;
};

trait EuclideanNorm() as Norm { self : Point =>
  def norm () : Int = self.x() + self.y();
};

trait ManhattanNorm() { self : Point =>
  def norm () : Int = self.x() + 2 * self.y();
};
